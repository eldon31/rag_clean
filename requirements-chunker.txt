# V5 Enhanced Ultimate Chunker - Complete Dependencies
# Install with: pip install -r requirements-chunker.txt

# ============================================================================
# CORE DEPENDENCIES (Required)
# ============================================================================

# Python tokenization and encoding
tiktoken>=0.5.0

# Scientific computing
numpy>=1.24.0
scikit-learn>=1.3.0

# ============================================================================
# OPTIONAL DEPENDENCIES (With Fallbacks)
# ============================================================================

# Semantic Scoring (Optional - Chunker works without this)
# Fallback: Heuristic coherence scoring
sentence-transformers>=2.2.0

# Tree-sitter Code Parsing (Optional - For AST-based code chunking)
# Fallback: Structural sentence-based chunking
tree-sitter>=0.20.0
tree-sitter-languages>=1.8.0

# Semchunk Semantic Boundaries (Optional - For smart text chunking)
# Fallback: Structural sentence-based chunking
semchunk>=2.0.0

# Docling Document Conversion (Optional - For PDF/Office/HTML)
# Fallback: Standard text extraction
docling>=1.0.0

# ============================================================================
# MODEL REGISTRY (Optional - For model-aware chunking)
# ============================================================================

# Note: kaggle_ultimate_embedder_v4.py provides KAGGLE_OPTIMIZED_MODELS
# Fallback: Uses default chunk sizes (1024 tokens)

# ============================================================================
# INSTALLATION TIERS
# ============================================================================

# Tier 1: Minimal (CPU-only, no optional features)
#   pip install tiktoken numpy scikit-learn

# Tier 2: Standard (CPU + code/text chunking)
#   pip install -r requirements-chunker.txt

# Tier 3: Full (GPU + semantic scoring)
#   pip install -r requirements-chunker.txt torch

# ============================================================================
# VERIFIED COMPATIBILITY
# ============================================================================

# Python: 3.9, 3.10, 3.11, 3.12
# OS: Windows, Linux, macOS
# Tested on: Windows 11, Kaggle T4 x2