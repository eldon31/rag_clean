# 🚀 Ultimate Kaggle Embedder Requirements V2
# Optimized for T4 x2 GPU Environment with SOTA Models & Reranking

# Fix numpy/sklearn binary compatibility (Critical for Kaggle)
numpy==1.26.4
scikit-learn==1.4.2

# Core ML Libraries
torch>=2.0.0
torchvision>=0.15.0
sentence-transformers>=2.2.2
transformers>=4.35.0

# SOTA Embedding Models Support
# Primary: nomic-ai/CodeRankEmbed (768D, fast, Kaggle optimized)
# SOTA: Alibaba-NLP/gte-Qwen2-7B-instruct (3584D, best quality)
# Balanced: BAAI/bge-m3 (1024D, multilingual)
# Speed: sentence-transformers/all-MiniLM-L6-v2 (384D, ultra fast)

# Reranking Models
# ms-marco-MiniLM-L-6-v2: Fast, good quality
# ms-marco-MiniLM-L-12-v2: Better quality, slower
# stsb-distilroberta-base: General purpose

# Vector Database
qdrant-client>=1.7.0
faiss-cpu>=1.7.4  # or faiss-gpu for GPU acceleration

# Data Processing
pandas>=2.0.0
tqdm>=4.65.0
psutil>=5.9.0

# Document Processing
docling>=2.0.0
docling-core>=2.0.0

# Utility
pydantic>=2.0.0
python-dotenv>=1.0.0

# Optional: ONNX Runtime for optimization
# onnxruntime-gpu>=1.16.0  # GPU acceleration
# optimum>=1.14.0          # Model optimization

# Optional: Flash Attention for large models (7B+)
# flash-attn>=2.3.0        # For gte-qwen2-7b, e5-mistral-7b

# Development/Testing
pytest>=7.4.0
jupyter>=1.0.0
ipywidgets>=8.0.0
